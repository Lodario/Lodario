import type { TeamPlayerDataset } from '@/components/coach/players/types';

export const analyticsLegendItems = [
  'Readiness trend',
  'Energy vs fatigue vs acute load',
  'Sleep timing and sleep quality',
  'Stress vs sleep score',
  'Multi-factor readiness inputs',
];

export function getTeamPlayers(_teamId: string): TeamPlayerDataset[] {
  return [];
}
